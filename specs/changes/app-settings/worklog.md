# Worklog - app-settings (US-3.5)

Per-change trail (`TRACE-001`). Entries **newest-first**, one line each:

`time · actor · action · artifact · ref`

Story ids only, no tracker UUIDs (public-repo hygiene).

---

## Entries

- `~00:55 · orchestrator/Fable · review gate PASS after fixing the blocker (re-verified: 422 backend +3, 99.57% cov, lint/format/typecheck clean; fe tsc+169 tests+prettier clean). scope=PASS (exact, all AC + ratified decisions honored, no creep). security=CLEAN (mass-assignment guarded by extra=forbid; added a regression test locking it; SEC-008 logging gap is project-wide carry-forward). code-review=HIGH H-1 FIXED (tautological always-truthy PUT asserts in the AC3 critical test B-I18 -> now assert .status_code==200) + M-2 dead helper removed + L-1 FE load resets saved-status + L-2 my em-dash in tasks.md fixed. test-engineer=APPROVED-WITH-WATCH (all cases present, 4 critical paths mutation-killed, regression guard intact). DEFERRED to tech-debt (REV-003): uv.lock drift (#64), no committed e2e specs (#63, PO-labelled debt). M-3 duplicate scenario-ID labels across test modules + security LOW-2 superseded (added the extra-key test) noted; M-3 left as comment-hygiene defer (parentheticals already disambiguate assembly vs unit). · specs/changes/app-settings · QG-004/REV-008/REV-003`
- `~00:20 · orchestrator/Fable · WATCH (PRIN-VIII, for retro): no committed Playwright/e2e spec files exist anywhere in the project; every prior UI story (plant-crud, care-events) and this one satisfy "Playwright acceptance" (TEST-009/CI-010, DoD §3) via a LIVE production-path smoke + committed breakpoint screenshots, not specs running in CI. US-3.5 matched that standing pattern (live MCP-browser run, zero console errors, persistence verified on reload, 4 screenshots). The absent committed-e2e-in-CI is a pre-existing systemic gap, not introduced here - candidate test-infra item for the retro. · specs/changes/app-settings/screenshots · TEST-009/CI-010/QG-004`
- `~00:15 · build-agent-FE/Fable · frontend lane GREEN (orchestrator re-verified: tsc clean, 169 fe tests +9, lint+prettier clean, build OK largest chunk 217kB < 300kB FE-007). Built lib/api/settings.ts (mirrors careSchedules client) + SettingsPage (load, seasonal-aware checkbox, 4 month/day inputs, return-to-default resets window only/toggle untouched, Save->PUT w/ role=status + role=alert), wired /settings route (replaced PlaceholderPage; nav link pre-existing). Production-path verified: built SPA served through backend on a fresh 0007-migrated SQLite DB, drove via Playwright MCP - lazy default rendered, toggled off + window May1-Sep1 saved (PUT 200), reload persisted, ZERO console errors/warnings at 1280/820/390. Screenshots committed: settings-{phone-390,tablet-820,desktop-1280,saved-1280}.png. NOT committed. · specs/changes/app-settings/screenshots · TEST-010/TEST-014/FE-012`
- `~00:05 · build-agent-FE/Fable · TEST-014 RED recorded before implementation: vitest 2 suites failed - Failed to resolve import "./settings" (settings.test.ts) and "./SettingsPage" (SettingsPage.test.tsx), "no tests". Implementation followed to green. · specs/changes/app-settings · TEST-014/PRIN-III`
- `~23:35 · build-agent-BE/Fable · backend lane GREEN (orchestrator re-verified: 419 passed +46, 99.57% cov, lint/typecheck/imports clean). Built domain/app_settings.py (SeasonalSettings + repo Protocol), application/settings.py (AppSettingsService lazy-default + ServiceSeasonalSettingsProvider), app_settings_repository (portable select-then-insert/update singleton id=1, never a 2nd row), migration 0007 (no seed, drops on downgrade), web settings router GET/PUT + schemas (month-aware day validator, no id/updated_at leak). due.py: additive seasonal_aware param + _due_from shared tail, OFF short-circuits to plain interval (never null), WinterWindow.contains UNTOUCHED (AC4 reused). DueQueryService reads settings once/query (N+1 bound preserved). US-3.3 due tests updated to pass seasonal_aware=True (on-path regression guard). OpenAPI: GET/PUT /settings present, schedules shape unchanged. NOT committed. DEVIATION (comply): reverted pre-existing uv.lock-vs-pyproject drift to committed state (not US-3.5; flagged for a separate uv lock refresh). · specs/changes/app-settings · TEST-014/ARCH-011/SPEC-001`
- `~23:05 · build-agent-BE/Fable · TEST-014 RED recorded before implementation: collection errors (ImportError SeasonalSettingsProvider from application.due; ModuleNotFoundError app_settings_repository), TypeError compute_due() unexpected kwarg seasonal_aware, AssertionError 'app_settings' not in table names; first red run new/edited files = 33 failed, 13 passed. Implementation followed to green. · specs/changes/app-settings · TEST-014/PRIN-III`
- `~22:50 · test-engineer/Fable · test-foundation written: 41 cases (10 BE unit incl. seasonal_aware=False matrix + US-3.3 on-path equivalence, 18 BE integration incl. singleton-upsert/migration/dual-engine/validation, 9 FE, 4 Playwright acceptance @390/1280); 4 critical-100% paths (off-branch, on-path equivalence, singleton-no-duplicate, month/day validation) w/ named mutation probes; AC1-AC6 traced; flagged shared-singleton test independence + additive-param regression-guard + AC4-contains-is-out-of-scope · specs/changes/app-settings/test-foundation.md · SPEC-003/TEST-001/TEST-007`
- `~22:45 · orchestrator/Fable · DoR gate PASS (13/13, no WATCH): two disjoint lanes (~250 BE / ~200 FE), additive contract, ARCH-011 portable singleton + migration, only US-3.3 edit is the additive seasonal_aware param + provider swap; UI story -> screenshots/ scaffolded · specs/changes/app-settings/proposal.md · QG-011/QG-004`
- `~22:30 · orchestrator/Fable · change proposed: branch feat/us-3.5-app-settings off main; product-spec amended (§3 seasonal-aware toggle + §4 US-3.5) recording the PO ratification 2026-06-13; proposal+design+tasks+screenshots scaffolded. Ratified decisions: toggle ON by default; OFF ignores winter-interval AND paused (plain interval); US-3.5 = settings page + persistence + due wiring, indicator UI deferred to #61, no climate questionnaire · specs/changes/app-settings/ · SPEC-001/SPEC-002/QG-001`
