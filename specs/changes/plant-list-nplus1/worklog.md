# Worklog - plant-list-nplus1 (tech debt)

`time · actor · action · artifact · ref` (newest first). Story/board ids only, no tracker UUIDs.

---

## Entries

- `~18:10 · orchestrator/Fable · review gate PASS after fixing the blocker. 3 reviewers: security=CLEAN; scope=exact-but-blocked-on HIGH (uv.lock ride-along) -> REVERTED (git checkout backend/uv.lock; the #64 drift stays its own ticket, not this change); code-review 0 CRITICAL/HIGH, MEDIUM-2 (_IntList/_IntToInt aliases) -> removed, but the reviewer's "false premise" was itself wrong: mypy DOES resolve list[int] to PhotoService.list (the .list method shadows the builtin in annotation scope) - fixed properly with Sequence[int] param + list() at the repo boundary; LOW-1 (dict(rows) non-determinism) -> added ORDER BY plant_id,id. Deviations recorded in proposal.md (lean track, CoverThumb export, write-path null). Re-verified: backend 437 passed/99.58%/lint+format+typecheck+imports clean; FE tsc+172 tests green. FE production path (separate lane) confirmed zero per-card photo requests + screenshot. · specs/changes/plant-list-nplus1 · QG-004/REV-008/PRIN-IX`
- `~11:15 · build-agent-BE/Fable · backend lane GREEN (orchestrator re-verified: 437 passed +14, 99.58% cov, lint/typecheck/imports clean). plant_repository: _load_tags_batch (one grouped query) used in the single filter-driven list (serves active + archived via PlantFilter; single-row reads keep _load_tags, no N+1). photo_repository.cover_ids_for_plants (one SELECT plant_id,id WHERE is_cover AND plant_id IN, empty-safe, dual-engine) + Protocol + thin PhotoService delegate. PlantResponse += additive cover_photo_id:int|None; plants router composes it into list (one batch call) + detail, NOT off the domain Plant (ARCH-006, mirrors schedules). Domain Plant untouched. TEST-014 red captured before impl (12 failed incl. assert 6<=3 N+1 proof, missing cover_ids_for_plants, KeyError cover_photo_id). NOT committed. · specs/changes/plant-list-nplus1 · TEST-014/ARCH-006/ARCH-011`
- `~01:10 · orchestrator/Fable · change opened: tech-debt pass eliminating the two plant-list N+1s (backend tag-load + frontend CoverThumb per-card fetch). Surfaced + triaged this session (board #62 broadened, #41 CoverThumb sub-item). Design folded into proposal.md; additive cover_photo_id on PlantResponse composed by the router (US-3.3 pattern); batch tag + cover reads, dual-engine. Branch refactor/plant-list-nplus1 off main · specs/changes/plant-list-nplus1/ · PRIN-IX/ARCH-006/ARCH-011`
