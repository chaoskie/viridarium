# Worklog - today-view (US-4.1)

`time · actor · action · artifact · ref` (newest first). Story/board ids only, no tracker UUIDs.

---

## Entries

- `2026-06-15 ~19:15 · orchestrator/Fable · review gate PASS after fixes (re-verified: frontend-only, no backend touched; tsc + lint + prettier + build clean; 219 fe tests, ZERO act() warnings). scope=CONDITIONAL-PASS->resolved (spec-exact, no backend/contract change, #66 deferred, HealthBadge deletion blessed as dead-code); test-engineer=APPROVED (all U/C cases present, U-1 parametrized, 9 mutation probes red, screenshots match foundation set). code-review=1 HIGH + MEDIUMs, all FIXED: HIGH-1 C-16 act() dangling update -> awaited settle, warning gone; M-1 partial Both failure could duplicate water -> logTypes now drops succeeded types before surfacing the error + new C-17 (red-then-green proven); M-2 button a11y -> aria-label '<action> <plant>' (visible text unchanged, WCAG 2.5.3 held); M-3 stale README HealthBadge line updated; L-1 standalone <li> wrapped in <ul> in tests. DEFERRED: L-2 React key by location name (names backend-unique, low). WATCH (retro, project-wide #63): acceptance is live-smoke+screenshots not CI e2e; FE perf budget is build-number not a failing assertion (FE-015). TEST-014 red transcript upgraded above. · specs/changes/today-view · QG-004/REV-008/REV-003`
- `2026-06-15 ~18:40 · build-agent-FE/Fable · frontend lane GREEN (orchestrator re-verified: frontend-only - NO backend file touched, uv.lock clean; tsc + lint + prettier clean; 218 fe tests; build 223kB < 300kB FE-007). Added ScheduleDue + Plant.schedules (additive, water|feed closed). buildTodayGroups pure derivation (classification, fan-out, group-by-location homeless-last, worstOverdue, two-level sort; today injected). TodayPage replaced placeholder (load plants+locations, groups/badges/empty/loading/error). TodayCard: Water/Feed per due type + Both iff exactly {water,feed}; one-tap createEvent happened_on=today, live card update no reload, in-flight disable, inline error; NO feeding-mode coupling (#66 deferred, Both=2 independent calls). TEST-014 red transcript (before impl): buildTodayGroups.test.ts + TodayCard.test.tsx = 'Failed to resolve import ./buildTodayGroups' / './TodayCard' (0 tests collected); TodayPage.test.tsx 6 failed (C-1,C-2,C-3,C-4,C-6,C-7) first failure 'TestingLibraryElementError: Unable to find role=heading name /^Bath$/i' (placeholder had no location groups). Production path: built SPA through backend, seeded due/overdue/homeless + a both-due plant, Playwright MCP - grouped Bath>Kitchen>No-location, counts match overdue_days, Both only on the both-due plant, one-tap drops the type live + persists, empty state after clearing, ZERO console errors at 390/1280. Deviations: deleted orphaned HealthBadge.tsx (only the placeholder used it, PRIN-I dead-code); edited App.test.tsx root-route smoke (placeholder replaced). NOT committed. · specs/changes/today-view/screenshots · TEST-014/TEST-010/FE-012`
- `2026-06-15 ~18:05 · test-engineer/Fable · test-foundation written: 32 cases (11 unit buildTodayGroups incl. M-CLS classification matrix + M-GRP grouping, 16 component TodayPage/TodayCard, 5 acceptance @390/1280); 4 critical paths (attention-set classification w/ null+future exclusion, both-due->both-care-types, group/homeless ordering, one-tap-updates-without-reload) w/ mutation probes; button-set rule pinned (Water iff water due, Feed iff feed due, Both iff exactly {water,feed}). · specs/changes/today-view/test-foundation.md · SPEC-003/TEST-007`
- `2026-06-15 ~17:50 · orchestrator/Fable · change proposed: branch feat/us-4.1-today-view off main; proposal+design+tasks+screenshots scaffolded. PO-resolved UX: grouped-by-location with inline overdue styling; frontend-only over the existing GET /plants (schedules+due from US-3.3) + /locations; one-tap Water/Feed per due care type + a Both button when both due. Feeding-mode coupling (liquid covers water) deferred to candidate #66. Frontend-only - no backend change. · specs/changes/today-view/ · SPEC-001/SPEC-002/QG-001`
