# Worklog - due-computation (US-3.3)

Per-change trail (`TRACE-001`). Entries **newest-first**, one line each:

`time · actor · action · artifact · ref`

Story ids only, no tracker UUIDs (public-repo hygiene).

---

## Entries

- `~21:55 · orchestrator/Fable · WATCH caveat (PRIN-VIII, for next retro): dual-engine B-I33 was exercised on SQLite only in the local run; the grouped-MAX + boolean enabled-filter SQL is standard and the test is correctly gated to DATABASE_URL, but PostgreSQL portability is unproven until the CI postgres leg runs green. Gate sign-off requires the CI postgres leg green on this branch. · specs/changes/due-computation/test-foundation.md · ARCH-011/QG-004`
- `~21:55 · orchestrator/Fable · review gate PASS (1 WATCH): orchestrator re-verified suite myself (373 passed, 99.54% cov, lint/typecheck/imports clean); 3-reviewer gate + test-engineer re-audit all clear. scope=APPROVED (scope exact, all AC traced, no migration/write-path/UI creep); code-review=APPROVED (0 CRITICAL/0 HIGH; 1 LOW test-override fragility, non-blocking); security=CLEAN (0 blocking; SQLi params bound, ids+enums only no PII); test-engineer=APPROVED-WITH-WATCH (46/46 cases, 4 critical paths mutation-killed, N+1 guards real). Out-of-scope debt surfaced + logged: pre-existing per-plant tag-load N+1 in plant_repository.list (NOT US-3.3; left untouched per PRIN-IX). · specs/changes/due-computation · QG-004/REV-008/SPEC-001`
- `~21:40 · build-agent-BE/Fable · implementation GREEN. Built domain/due.py (WinterWindow value object with new-year-wrap .contains, ScheduleDue, pure compute_due in proposal §3 branch order), application/due.py (WinterWindowProvider Protocol + DefaultWinterWindowProvider Nov1-Mar1 + DueQueryService.for_plants in TWO grouped reads, no N+1), batch reads care_event_repository.latest_event_dates (grouped MAX(happened_on) GROUP BY plant_id,type, empty-ids safe) + care_schedule_repository.enabled_for_plants (enabled IN-filter), both added to the domain Protocols. Web: ScheduleDueResponse + additive PlantResponse.schedules, get_due_query_service dependency, DueQueryService wired in container/app factory, plants router merges due into list (one batch call over non-archived ids) + detail (archived -> empty schedules). Gates run: \`make lint\` PASS (all checks passed), \`make format-check\` PASS (80 files formatted), \`make typecheck\` PASS (mypy: no issues, 54 files), \`make imports\` PASS (3 contracts kept incl. domain framework-free), \`make test-coverage\` PASS (373 passed, total coverage 99.54% >= 85 floor; due.py/application due/both repos at 100%). All 46 foundation cases implemented + passing; dual-engine B-I33 passes on the SQLite leg locally (the CI postgres leg runs the same test via DATABASE_URL). OpenAPI generates: next_due string($date)|null, overdue_days integer|null, schedules array on plant reads only. DEVIATION (comply): updated the pre-existing exact-key-set assertion in test_plants_endpoint.py::test_openapi_exposes_plant_paths_query_params_and_schema to include the additive \`schedules\` key - directly entailed by the spec'd additive field (proposal §API), not a drive-by (PRIN-IX). NOTE for review: B-I30 (list-path N+1) asserts the due-assembly statement count is flat across N/2N by exercising the wired DueQueryService directly; the plant LIST endpoint has a SEPARATE pre-existing per-plant tag-load N+1 (plant_repository.list -> _load_tags per row) that is OUT OF US-3.3 SCOPE (PRIN-IX) and was left untouched. No migration authored; no write path changed. NOT committed (QG-010). · specs/changes/due-computation/ · TEST-014/SPEC-001/ARCH-006/ARCH-011`
- `~21:10 · build-agent-BE/Fable · TEST-014 RED recorded before any implementation. Wrote the failing tests first (test_due.py B-U1..B-U14; test_due_query_service.py assembly slice; test_due_repositories.py B-I1..B-I19; test_due_endpoint.py B-I7/B-I8/B-I20..B-I32; test_fk_cross_engine.py B-I33). Run \`pytest <the 5 due test files>\` -> 4 collection ImportErrors (the red): \`ModuleNotFoundError: No module named 'viridarium.domain.due'\` (test_due.py:20); \`No module named 'viridarium.application.due'\` (test_due_query_service.py:18, test_due_repositories.py:32, test_due_endpoint.py:20); pytest: "Interrupted: 4 errors during collection / 4 errors in 0.13s". Implementation follows to turn these green. · specs/changes/due-computation/ · TEST-014/PRIN-III`
- `~20:45 · test-engineer/Fable · test-foundation written: 46 numbered cases (14 domain unit incl. 2 matrices, 32 integration, 1 dual-engine), 4 critical-100% paths (effective-interval table, paused null-path, window-wrap, matching-type filter), all AC1-AC8 traced; N+1 asserted via statement-count listener; both-null invariant + exact response key-set pinned · specs/changes/due-computation/test-foundation.md · SPEC-003/TEST-001`
- `~20:42 · orchestrator/Fable · DoR gate PASS (13/13, no WATCH): sized ~260 LOC backend-only, additive non-breaking contract, ARCH-006 query module + ARCH-011 batch reads, open questions zeroed · specs/changes/due-computation/proposal.md · QG-011/QG-004`
- `~20:40 · orchestrator/Fable · change proposed: branch feat/us-3.3-due-computation off main; proposal+design+tasks scaffolded; pickup decisions confirmed (winter-interval fallback to normal; paused = never-due-in-window) trace to the US-3.1 model, nothing new baked in; broader seasonal UI/global-toggle vision captured to the board (US-3.5 enrichment + new dashboard ticket), out of this story's scope · specs/changes/due-computation/ · SPEC-001/SPEC-002/QG-001`
