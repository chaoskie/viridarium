# Worklog - care-timeline (US-3.4)

`time · actor · action · artifact · ref` (newest first). Story/board ids only, no tracker UUIDs.

---

## Entries

- `2026-06-15 ~15:30 · orchestrator/Fable · review gate (3 reviewers + test re-audit): security=CLEAN; scope=PASS (exact, no creep, US-4.3 content absent); test-engineer=APPROVED-WITH-WATCH (all 22 BE + 12 FE cases present, 4 critical paths mutation-killed). code-review=3 HIGH, all FIXED this pass: HIGH-1 B-I4 tautological tiebreak -> root cause is second-granular integration created_at (same-second items can't be tiebroken), so B-I4 honestly rewritten to assert deterministic STABLE order + the tiebreak KEY stays pinned at unit B-U5 (mutation-verified); HIGH-2 FE-lane red/worklog absent -> recorded below; HIGH-3 2 of 4 FE-012 screenshots missing -> recaptured (tablet-820 populated + empty-390). MEDIUM PRIN-I merge_timeline length -> inline justification added. LOW B-I17 real-clock dependency -> events moved to past May dates. DEFERRED (REV-003): TEST-007 parametrize of test_timeline_merge - the 5 B-U cases are distinct PROPERTY tests (dedup/standalone-date/interleave/backdated/tiebreak), not cells of one uniform input matrix, so parametrize would obscure not clarify; test-engineer (TEST-007 authority) concurred by not flagging it. WATCH (retro): A-5 perf budget is vite chunkSizeWarningLimit (warning-only, not a failing assertion) - a project-wide FE-015 posture, not introduced here, relates to #63. Re-verified: 459 backend +22 / 99.60%, lint/format/typecheck/imports clean; FE tsc + tests green. · specs/changes/care-timeline · QG-004/REV-008/REV-003`
- `2026-06-15 ~15:00 · build-agent-FE/Fable · frontend lane GREEN (orchestrator re-verified: tsc clean, 184 fe tests +12, lint/prettier clean, build OK 221kB < 300kB FE-007). lib/api/timeline.ts (discriminated union narrowed on kind) + CareTimeline (newest-first, distinct per-type marker glyph+label NOT colour-only, observe health, inline + standalone photos, empty state) + minimal PlantDetailPage (/plants/:id route, name header + back, US-4.3 precursor - NO attributes/schedules/gallery) + per-plant link in PlantsPage. TEST-014 RED recorded before impl: vitest 4 suites failed - Failed to resolve import ./timeline, ./CareTimeline, ./PlantDetailPage + role=link 'view fern's history' not found. Production path: built SPA through backend on fresh 0007 DB, seeded mixed history, Playwright MCP - feed renders newest-first w/ distinct types, observe health, linked photo inline once (dedup correct), standalone photo present, empty state for fresh plant, ZERO console errors at 390/1280. NOT committed. · specs/changes/care-timeline/screenshots · TEST-014/TEST-010/FE-012`
- `2026-06-15 ~14:05 · build-agent-BE/Fable · backend lane GREEN (orchestrator re-verified: 459 passed +22, 99.60% cov, lint/typecheck/imports clean; timeline.py 100%). application/timeline.py: TimelineQueryService.for_plant + pure merge_timeline + framework-free tagged shapes (ARCH-006 read-only; reuses CareEventRepository.list_for_plant + PhotoRepository.list_for_plant + plant_exists guard FIRST; NO new repo method, NO migration). web: discriminated event|photo schema + GET /plants/{id}/timeline router + dependency + container/app wiring. Dedup: event-linked photos inline-only; standalone -> kind:photo (interleave confirmed). Sort (date, created_at) desc; date=happened_on for events, created_at.date() for photos. 404 plant-reason no PII, guard-first. TEST-014 red recorded before impl (ModuleNotFoundError timeline + 16 endpoint fails incl. missing-route KeyError). 4 critical paths mutation-probed; WATCH: integration tiebreak B-I4 survives the tiebreak-mutation via stable-sort coincidence, determinism pinned at unit B-U5 instead (prod sort key correct). Dual-engine B-I17 SQLite-local; PG on CI leg. NOT committed. · specs/changes/care-timeline · TEST-014/ARCH-006/ARCH-011`
- `2026-06-15 ~13:40 · orchestrator/Fable · build pickup: PO confirmed standalone (no-event) photos INTERLEAVE into the timeline by date (proposal updated, foundation flip resolved to interleave). DoR re-affirmed (artifacts present, decisions resolved, no migration, deps merged). Starting BE lane then FE lane. · specs/changes/care-timeline · QG-011/QG-004`
- `2026-06-14 ~22:50 · test-engineer/Fable · test-foundation written (pre-stage): 39 cases (17 BE integration, 5 BE unit pure-merge, 12 FE, 5 acceptance) + matrix M-TL; 4 critical-100% paths (event-photo dedup invariant, backdated-sort-by-happened_on, missing-plant-404-reason, union-shape contract) w/ named mutation probes; response contract pinned verbatim; residual standalone-photo assumption carried with a build-time FLIP instruction (default=interleave). · specs/changes/care-timeline/test-foundation.md · SPEC-003/TEST-007`
- `2026-06-14 ~22:35 · orchestrator/Fable · PRE-STAGED for next session: branch feat/us-3.4-care-timeline off main; proposal+design+tasks+screenshots scaffolded; PO answered the placement questions (#13) - timeline on a minimal /plants/{id} details page (US-4.3 precursor), API-first GET /plants/{id}/timeline, event photos shown inline once (photobook filed separately as a candidate). One residual assumption flagged (standalone photos interleave by date, default yes). Build NOT started; test-foundation requested next. · specs/changes/care-timeline/ · SPEC-001/SPEC-002`
