/**
 * External project links (US-? / VIRIDARIUM-76). Centralised so the footer and
 * About page share one source of truth. These are user-initiated `<a>`
 * navigations only - the app makes no automatic outbound calls (PRIN-II).
 */

/** The maintainer's aggregated support / social links (BuyMeACoffee, PayPal, etc.). */
export const SUPPORT_URL = "https://linktr.ee/chaoskie";

/** The public source repository. */
export const REPO_URL = "https://github.com/chaoskie/viridarium";

/** Anchor to the README "A note from the maintainer" section. */
export const MAINTAINER_NOTE_URL = `${REPO_URL}#a-note-from-the-maintainer`;
