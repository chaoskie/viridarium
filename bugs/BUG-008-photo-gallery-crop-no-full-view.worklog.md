# Worklog - BUG-008 photo gallery crop + full-image view

Per-fix trail (`TRACE-001`). Entries newest-first: `time · actor · action · artifact · ref`.

## Entries

- `14:40 · claude/OPUS · GREEN + evidence: in-modal full-image view added; thumbnails are buttons opening the uncropped image (object-contain, max-h 60vh) with "Back to all photos". Full FE gate green: lint/format/typecheck, 236 vitest, build. e2e 16/16 incl. 2 new photo-gallery screenshot specs. FE-012 screenshots (grid cropped square vs full uncropped) committed for S25+ and desktop · PhotoGalleryModal.tsx, bugs/BUG-008-photo-gallery-screenshots/ · TEST-009/FE-012`
- `14:39 · claude/OPUS · e2e infra: run-backend.sh now exports a throwaway PHOTOS_DIR under frontend/.e2e/photos (default /data/photos isn't writable in dev/CI; no prior e2e exercised photo upload). Generated a 640x240 fixture PNG (e2e/fixtures/sample-photo.png) so the crop-vs-contain difference is visible · e2e/run-backend.sh, e2e/fixtures/sample-photo.png · TEST-009`
- `14:33 · claude/OPUS · RED->GREEN (TEST-014): new PhotoGalleryModal tests (open full image on thumbnail select; Back returns to grid) failed against unfixed code with: "TestingLibraryElementError: Unable to find an accessible element with the role \"button\" and name \`/view this photo .* at full size/i\`" (2 failed). Green after the in-modal view-toggle (2 passed). Design choice recorded: NOT a nested Modal (two dialogs both bind a document Escape listener -> one Esc would close both); in-modal full view reuses the existing Modal (FE-010) · PhotoGalleryModal.test.tsx, PhotoGalleryModal.tsx · PRIN-III/FE-010`
- `14:42 · claude/OPUS · REVIEW GATE: security PASS (0) / scope (5 unrelated re-captured screenshots reverted, F-001 cleared; ACs all met; Pillow generation correctly deferred) / code CONDITIONAL->PASS (TEST-014 red output added above; no CRITICAL/HIGH). LOW (logged as TD, advisory): identical thumbnail aria-labels across photos (add ordinal/date disambiguation later); first "image button" archetype could warrant an ADR if reused · /review · QG-004`
- `14:31 · claude/OPUS · intake: filed BUG-008 from template (severity medium, evidence reproduced); dedupe vs BUG-001..007 = none. Server-side thumbnail generation (Pillow) deferred - needs a stack amendment (PRIN-V); ticket fix direction allows "generated OR constrained" · bugs/BUG-008-photo-gallery-crop-no-full-view.md · VIRIDARIUM-73`
