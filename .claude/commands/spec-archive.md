---
description: Close a change - DoD gate, change index, then move it to specs/archive
---
Archive change: $ARGUMENTS

1. **DoD gate (`QG-012`):** run the DoD checklist (`templates/dod.md`) - aggregates mechanical gate (`QG-001`), coverage (`QG-002`), gate-checks posted (`QG-004`), sign-off (`QG-005`), worklog complete (`TRACE-007`), pipeline green (PRIN-VII), security review done if feature-boundary (`SEC-010`). Post PASS/WATCH/FAIL per item; any FAIL → stop; any WATCH → caveat worklogged for the next retro.
2. **Move the change (`SPEC-002`):** move `specs/changes/<change-name>/` to `specs/archive/<change-name>/` (worklog travels with it, `TRACE-001`).
3. **Change index (`TRACE-002`):** add the one-glance entry (what / why / link) newest-first to the change index if one exists under `.claude/docs/`.
4. Changelog is generated by CI from Conventional Commits (`CI-008`) - verify, don't hand-write.
5. Update the work item in the project tracker per docs in `.claude/docs/` if present.
6. **Distill prompt (`LANG-008`):** offer a knowledge sync + `/retrospective` for learnings from this change.
7. Log the archive transition to the worklog (`TRACE-003/004`).
